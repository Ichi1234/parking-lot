class ParkingLot {
    constructor(levels) {
        this.level = levels;
        this.NUM_LEVELS = 5;
    }

    ParkingLot() {
        levels = this.level[this.NUM_LEVELS];
        for (let i = 0; i < this.NUM_LEVELS; i++) {
            levels[i] = new 
        }
    }
}